
@{
    ViewBag.Title = "New Blog";
}

<h2>@ViewData["Title"]</h2>

<form asp-controller="Home" asp-action="Destroyyy" method="post" class="form-horizontal" role="form">
    <div class="form-horizontal">
        <p>Are you sure? This action will permanently wipe out the database.</p>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Destroy" class="btn btn-default" />
            </div>
            @Html.ActionLink("Return", "Index", "Test");
        </div>
    </div>
</form>
